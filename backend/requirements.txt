# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
alembic==1.13.1

# Auth
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
python-multipart==0.0.9

# Redis
redis==5.0.4

# RabbitMQ
aio-pika==9.4.1
pika==1.3.2

# AI / LangGraph
langgraph==0.1.5

# LLM providers
groq>=0.9.0                  # primary: llama-3.3-70b-versatile
google-generativeai>=0.8.0   # optional fallback

# Validation and config
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1

# Language detection
langdetect==1.0.9

# Embeddings and vector search
sentence-transformers==2.7.0
numpy>=1.26.0

# PDF extraction
PyMuPDF>=1.24.0              # primary: text + layout from digital PDFs
pdfplumber==0.11.0           # table extraction

# Document processing
python-docx>=1.1.0
openpyxl>=3.1.0

# Data handling
pandas>=2.2.0
jsonlines>=4.0.0

# HTTP
httpx>=0.27.0,<0.28.0

# ── DEFERRED — do not install yet ────────────────────────────────────────────
# camelot-py[base]   # complex tables — needs Ghostscript installed first
# paddlepaddle       # OCR engine — ~500 MB download
# paddleocr          # multilingual OCR — ~1 GB models on first run
